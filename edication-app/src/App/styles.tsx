import {jss} from 'react-jss'

export const useStyles = jss.createStyleSheet({
    AppComponent: {
        display: 'flex',
        flexDirection: 'column',
        height: '100%',
        minHeight: '100vh',
        width: '100%',
    }
});
