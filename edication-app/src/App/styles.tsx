import {jss} from 'react-jss'

export const useStyles = jss.createStyleSheet({
    AppComponent: {
        display: 'flex',
        flexDirection: 'column',
        height: '100%',
        width: '100%',
    }
});
