import {jss} from 'react-jss'

export const stylesList = jss.createStyleSheet({
    AppComponent: {
        display: 'flex',
        flexDirection: 'column',
        height: '100%',
        minHeight: '100vh',
        width: '100%',
    }
});
